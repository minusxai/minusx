services:
  docs:
    build:
      # Repository-root context keeps frontend/compatibility.json as the one
      # compatibility contract shared by the app, setup script, and docs.
      context: ..
      dockerfile: docs/Dockerfile
    container_name: minusx-docs
    restart: unless-stopped
    ports:
      - "3001:3001"
