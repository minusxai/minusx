---
title: Dashboards
description: Combine questions into interactive dashboards
---

A **dashboard** is a collection of questions arranged in a grid layout. Dashboards give you a single view of multiple metrics, charts, and tables — with shared parameters for interactive filtering.

## What dashboards do

- **Aggregate insights** — see multiple questions side by side
- **Interactive filtering** — shared parameters let viewers filter all questions at once
- **Flexible layout** — drag and resize question cards in a grid
- **Shareable** — share a dashboard by its URL or ID

## When to use dashboards

Use a dashboard when you need to:

- Monitor a set of related metrics together
- Give stakeholders a self-service view they can filter
- Present a data story with multiple visualizations
