/**
 * Semantic explorer — the single-surface semantic exploration canvas
 * (fields rail + shelves + chart-type rail). Replaces the old SemanticCanvas
 * two-pane editor and the separate Viz tab for semantic-mode questions.
 */

export { SemanticExplorer } from './SemanticExplorer';
export { FieldsRail, type DraggingField } from './FieldsRail';
export { FilterEditor } from './FilterEditor';
export { SqlPeekDrawer } from './SqlPeekDrawer';
