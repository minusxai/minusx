/**
 * Completions module - typed autocomplete functionality
 *
 * Usage:
 *   Server-side: import { CompletionsAPI } from '@/lib/data/completions/completions.server'
 *   Client-side: import { CompletionsAPI } from '@/lib/data/completions/completions'
 *   Types: import { MentionsOptions, MentionsResult } from '@/lib/data/completions/types'
 */

export * from './types';
export * from './completions.interface';
