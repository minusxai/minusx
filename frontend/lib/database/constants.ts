/**
 * Database version constants for migrations
 */

export const LATEST_DATA_VERSION = 33;  // V33: Whitelist schema redesign (databases[] → whitelist tree) + default context per folder
export const LATEST_SCHEMA_VERSION = 8;
