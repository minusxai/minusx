diff --git a/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js b/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
index 2eda248..715b1da 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
@@ -85,13 +85,17 @@ function convertContentBlocks(content) {
                 text: sanitizeSurrogates(block.text),
             };
         }
+        // [MX PATCH] Support remote image URLs (Anthropic url source) in addition
+        // to base64, so callers can pass a URL without a server-side fetch.
         return {
             type: "image",
-            source: {
-                type: "base64",
-                media_type: block.mimeType,
-                data: block.data,
-            },
+            source: block.url
+                ? { type: "url", url: block.url }
+                : {
+                    type: "base64",
+                    media_type: block.mimeType,
+                    data: block.data,
+                },
         };
     });
     // If only images (no text), add placeholder text block
@@ -374,6 +378,7 @@ export const stream = (model, context, options) => {
                         const block = {
                             type: "text",
                             text: "",
+                            citations: [], // [MX PATCH] web-search citations accumulate here
                             index: event.index,
                         };
                         output.content.push(block);
@@ -414,6 +419,30 @@ export const stream = (model, context, options) => {
                         output.content.push(block);
                         stream.push({ type: "toolcall_start", contentIndex: output.content.length - 1, partial: output });
                     }
+                    // [MX PATCH] Web search (server tool). Keep server_tool_use
+                    // (with its streamed query) AND the web_search_tool_result so
+                    // the pairing can be re-sent on multi-turn — Anthropic
+                    // requires the server_tool_use that a web_search_tool_result
+                    // references. The chat-translator hides server_tool_use from
+                    // the UI; only web_search_tool_result + citations render.
+                    else if (event.content_block.type === "server_tool_use") {
+                        output.content.push({
+                            type: "server_tool_use",
+                            id: event.content_block.id,
+                            name: event.content_block.name,
+                            input: event.content_block.input ?? {},
+                            partialJson: "",
+                            index: event.index,
+                        });
+                    }
+                    else if (event.content_block.type === "web_search_tool_result") {
+                        output.content.push({
+                            type: "web_search_tool_result",
+                            tool_use_id: event.content_block.tool_use_id,
+                            content: event.content_block.content,
+                            index: event.index,
+                        });
+                    }
                 }
                 else if (event.type === "content_block_delta") {
                     if (event.delta.type === "text_delta") {
@@ -455,6 +484,12 @@ export const stream = (model, context, options) => {
                                 partial: output,
                             });
                         }
+                        // [MX PATCH] Accumulate the web-search query onto the
+                        // server_tool_use block so it can be re-sent on multi-turn.
+                        else if (block && block.type === "server_tool_use") {
+                            block.partialJson += event.delta.partial_json;
+                            block.input = parseStreamingJson(block.partialJson);
+                        }
                     }
                     else if (event.delta.type === "signature_delta") {
                         const index = blocks.findIndex((b) => b.index === event.index);
@@ -464,6 +499,14 @@ export const stream = (model, context, options) => {
                             block.thinkingSignature += event.delta.signature;
                         }
                     }
+                    // [MX PATCH] Web-search citations attach to the active text block.
+                    else if (event.delta.type === "citations_delta") {
+                        const index = blocks.findIndex((b) => b.index === event.index);
+                        const block = blocks[index];
+                        if (block && block.type === "text") {
+                            (block.citations = block.citations || []).push(event.delta.citation);
+                        }
+                    }
                 }
                 else if (event.type === "content_block_stop") {
                     const index = blocks.findIndex((b) => b.index === event.index);
@@ -498,6 +541,11 @@ export const stream = (model, context, options) => {
                                 partial: output,
                             });
                         }
+                        // [MX PATCH] Finalize the server_tool_use query.
+                        else if (block.type === "server_tool_use") {
+                            block.input = parseStreamingJson(block.partialJson);
+                            delete block.partialJson;
+                        }
                     }
                 }
                 else if (event.type === "message_delta") {
@@ -711,6 +759,19 @@ function buildParams(model, context, isOAuthToken, options) {
     if (context.tools && context.tools.length > 0) {
         params.tools = convertTools(context.tools, isOAuthToken, compat.supportsEagerToolInputStreaming, compat.supportsCacheControlOnTools ? cacheControl : undefined);
     }
+    // [MX PATCH] Anthropic native web search (server-side tool). Enabled via
+    // options.webSearch (boolean or { maxUses, userLocation: { city } }).
+    if (options?.webSearch) {
+        const ws = options.webSearch;
+        const webSearchTool = { type: "web_search_20250305", name: "web_search" };
+        if (ws && typeof ws === "object") {
+            if (ws.maxUses != null)
+                webSearchTool.max_uses = ws.maxUses;
+            if (ws.userLocation && ws.userLocation.city)
+                webSearchTool.user_location = { type: "approximate", city: ws.userLocation.city };
+        }
+        params.tools = [...(params.tools ?? []), webSearchTool];
+    }
     // Configure thinking mode: adaptive, budget-based, or explicitly disabled.
     if (model.reasoning) {
         if (options?.thinkingEnabled) {
@@ -785,13 +846,16 @@ function convertMessages(messages, model, isOAuthToken, cacheControl, allowEmpty
                         };
                     }
                     else {
+                        // [MX PATCH] Support remote image URLs (Anthropic url source).
                         return {
                             type: "image",
-                            source: {
-                                type: "base64",
-                                media_type: item.mimeType,
-                                data: item.data,
-                            },
+                            source: item.url
+                                ? { type: "url", url: item.url }
+                                : {
+                                    type: "base64",
+                                    media_type: item.mimeType,
+                                    data: item.data,
+                                },
                         };
                     }
                 });
@@ -864,6 +928,24 @@ function convertMessages(messages, model, isOAuthToken, cacheControl, allowEmpty
                         input: block.arguments ?? {},
                     });
                 }
+                // [MX PATCH] Re-send the web-search pairing on multi-turn so the
+                // model retains the search results (and Anthropic accepts the
+                // web_search_tool_result, which references the server_tool_use).
+                else if (block.type === "server_tool_use") {
+                    blocks.push({
+                        type: "server_tool_use",
+                        id: block.id,
+                        name: block.name,
+                        input: block.input ?? {},
+                    });
+                }
+                else if (block.type === "web_search_tool_result") {
+                    blocks.push({
+                        type: "web_search_tool_result",
+                        tool_use_id: block.tool_use_id,
+                        content: block.content,
+                    });
+                }
             }
             if (blocks.length === 0)
                 continue;
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses-shared.js b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses-shared.js
index cd76bb3..c098515 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses-shared.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses-shared.js
@@ -235,7 +235,10 @@ export async function processResponsesStream(openaiStream, output, stream, model
             return slot;
         }
         if (item.type === "message") {
-            const block = { type: "text", text: "" };
+            // [MX PATCH] `citations` accumulates web-search url_citation annotations
+            // (see response.output_text.annotation.added below), mirroring the
+            // Anthropic web-search patch so the frontend renders source chips.
+            const block = { type: "text", text: "", citations: [] };
             output.content.push(block);
             const slot = { type: "text", block, contentIndex: output.content.length - 1 };
             outputSlots.set(outputIndex, slot);
@@ -353,6 +356,22 @@ export async function processResponsesStream(openaiStream, output, stream, model
                 partial: output,
             });
         }
+        // [MX PATCH] Web-search url_citation annotations → text-block citations in the
+        // unified Anthropic-native shape (web_search_result_location) the frontend renders.
+        else if (event.type === "response.output_text.annotation.added") {
+            const slot = getSlot(event.output_index, "text");
+            const ann = event.annotation;
+            if (slot && ann && ann.type === "url_citation") {
+                (slot.block.citations = slot.block.citations || []).push({
+                    type: "web_search_result_location",
+                    url: ann.url,
+                    title: ann.title,
+                    cited_text: typeof ann.start_index === "number" && typeof ann.end_index === "number"
+                        ? slot.block.text.slice(ann.start_index, ann.end_index)
+                        : undefined,
+                });
+            }
+        }
         else if (event.type === "response.refusal.delta") {
             const slot = getSlot(event.output_index, "text");
             if (!slot)
@@ -416,6 +435,29 @@ export async function processResponsesStream(openaiStream, output, stream, model
             else if (item.type === "message" && slot?.type === "text") {
                 slot.block.text = item.content?.map((c) => (c.type === "output_text" ? c.text : c.refusal)).join("") || "";
                 slot.block.textSignature = encodeTextSignatureV1(item.id, item.phase ?? undefined);
+                // [MX PATCH] Merge web-search url_citation annotations from the final message
+                // content (authoritative + complete), deduped against any captured while
+                // streaming. Robust whether or not annotation.added events were emitted.
+                {
+                    const cites = (slot.block.citations = slot.block.citations || []);
+                    const seen = new Set(cites.map((c) => `${c.url}#${c.cited_text ?? ""}`));
+                    for (const part of item.content ?? []) {
+                        if (part.type !== "output_text" || !Array.isArray(part.annotations))
+                            continue;
+                        for (const ann of part.annotations) {
+                            if (!ann || ann.type !== "url_citation")
+                                continue;
+                            const cited_text = typeof ann.start_index === "number" && typeof ann.end_index === "number"
+                                ? slot.block.text.slice(ann.start_index, ann.end_index)
+                                : undefined;
+                            const key = `${ann.url}#${cited_text ?? ""}`;
+                            if (seen.has(key))
+                                continue;
+                            seen.add(key);
+                            cites.push({ type: "web_search_result_location", url: ann.url, title: ann.title, cited_text });
+                        }
+                    }
+                }
                 stream.push({
                     type: "text_end",
                     contentIndex: slot.contentIndex,
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
index b1a536b..1aa0a40 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
@@ -186,6 +186,16 @@ function buildParams(model, context, options) {
     if (context.tools && context.tools.length > 0) {
         params.tools = convertResponsesTools(context.tools);
     }
+    // [MX PATCH] OpenAI hosted web search (Responses API). Enabled via options.webSearch
+    // (boolean or { userLocation: { city } }). Mirrors the Anthropic web-search patch.
+    if (options?.webSearch) {
+        const ws = options.webSearch;
+        const webSearchTool = { type: "web_search" };
+        if (ws && typeof ws === "object" && ws.userLocation && ws.userLocation.city) {
+            webSearchTool.user_location = { type: "approximate", city: ws.userLocation.city };
+        }
+        params.tools = [...(params.tools ?? []), webSearchTool];
+    }
     if (model.reasoning) {
         if (options?.reasoningEffort || options?.reasoningSummary) {
             const effort = options?.reasoningEffort
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js b/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
index 7b08245..9c7399e 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
@@ -24,6 +24,9 @@ export function buildBaseOptions(model, context, options, apiKey) {
         maxRetries: options?.maxRetries,
         maxRetryDelayMs: options?.maxRetryDelayMs,
         metadata: options?.metadata,
+        // [MX PATCH] Forward web search through the simple-options path so the
+        // Anthropic provider's buildParams can inject the web_search tool.
+        webSearch: options?.webSearch,
         env: options?.env,
     };
 }
diff --git a/node_modules/@earendil-works/pi-ai/dist/utils/estimate.js b/node_modules/@earendil-works/pi-ai/dist/utils/estimate.js
index 17ef451..12dea36 100644
--- a/node_modules/@earendil-works/pi-ai/dist/utils/estimate.js
+++ b/node_modules/@earendil-works/pi-ai/dist/utils/estimate.js
@@ -39,7 +39,10 @@ export function estimateMessageTokens(message) {
             chars += block.thinking.length;
         }
         else {
-            chars += block.name.length + safeJsonStringify(block.arguments).length;
+            // [MX PATCH] Null-safe for web-search blocks (server_tool_use /
+            // web_search_tool_result) carried on assistant messages by the
+            // web-search patch — they don't have the toolCall shape.
+            chars += (block.name?.length ?? 0) + safeJsonStringify(block.arguments ?? block.content ?? block.input ?? {}).length;
         }
     }
     return Math.ceil(chars / CHARS_PER_TOKEN);
